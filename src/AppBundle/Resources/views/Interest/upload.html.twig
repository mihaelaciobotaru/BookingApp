{% extends "::base.html.twig" %}

{% block title %}{{ title }}{% endblock %}
{% block content %}
    <div class="col-md-9">
        <!-- general form elements -->
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">{{ title }}</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
            <!--    <div class="form-group">
                    <div class="col-sm-10">
                        <div data-dropzone="-1" class="dropzone dz-clickable" id="my-awesome-dropzone">
                            <div class="dropzone-previews"></div>
                            <div class="dz-default dz-message">
                                <h2><b>Drop files</b> to upload</h2>
                            </div>
                        </div>
                    </div>
                </div>

            </div>-->
                <form enctype="multipart/form-data" class="dropzone dz-clickable" data-dropzone="-1" id="my-awesome-dropzone" action="{{ path('interest_do_upload') }}">
                    <div class="dropzone-previews"></div>
                    <div class="dz-default dz-message">
                        <h1><b>Drop files</b> to upload</h1>
                    </div>
                    {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
            <!-- /.box-body -->
        </div>
        <!-- /.box -->
    </div>
{% endblock %}